# Conditions
if price >= 1.00:
    tax = .07
    print(tax)
else:
    tax = 0
    print(tax)

# Alternative Code:
if price >= 1.00:
    tax=.07
else:
    tax = 0
print(tax)

# > Greater than
# < Less than
# >= Greater than or equal to
# <= Less than or equal to
# == is equal to
# != is not equal to
